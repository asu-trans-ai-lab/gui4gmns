# gui4gmns / web-lite — zero-install QC viewer (v1 SHIPPED)
Single self-contained `../nexta_x.html` (kept at repo root so it can be copied into any run folder).
Scope: SHARED_CONTRACT §2 full; scale target <= ~50k links, hundreds of animated agents.
Status: v1 verified 2026-07-02 on the Chicago Sketch demo (`../demo/`).
