# NeXTA-X / python-lab — research scripting over the same contract
pandas readers for every contract file + matplotlib/pyqtgraph quick-views + notebook recipes
(link MOE maps, ODME before/after, gridlock event timelines). Shares column definitions with
dynamic-odme-lab diagnostics. This branch is also where contract conformance tests live (golden
numbers from the Chicago Sketch demo, cross-checked against web-lite).
