# ---- PRIVACY: agency data must NEVER be committed (NVTA / VDOT / INRIX / CBI / QVDF-corridor) ----
*PRIVATE*
*private*
!validate_no_private_data.py
**/06_nvta_am_PRIVATE/
**/*nvta*
**/*NVTA*
ai-gen/variants/nvta_corridor_contour.png
datasets/06_*/

# ---- generated / regenerable artifacts (users run the generator) ----
**/dashboard.html
**/dashboard_*.html
**/dashboard_layers/
**/portals/
**/export/
desktop-qt/snapshots/
desktop-qt/tile_cache/
demo/
web-gl/demo_regional/

# ---- large public networks: reference asu-trans-ai-lab/dynamic-odme-lab instead of duplicating ----
datasets/03_chicago_regional/
datasets/04_arc_atlanta/

# ---- reference-only material (other projects; different provenance/licensing) ----
html_examples/

# ---- engine binaries (source is versioned in engine/DLSim_STE; build with build.sh) ----
engine/bin/
*.exe
engine/DLSim_STE/testdata/*/out/
engine/DLSim_STE/testdata/*/out_*.csv

# ---- python / editor cruft ----
__pycache__/
*.pyc
.idea/
.vscode/
.claude/
.DS_Store

# ---- dev-workspace only (names private data; must never reach the public release) ----
DEV_README.md

# ---- ITS I-95: only the 2.3 GB RAW source stays out; the extracted _sample IS published ----
# Owner cleared the extracted sample as public (USDOT JPO CodeHub ITS demonstration data, 2026-07-06):
# the _sample folder (network + INRIX/RITIS speeds + VDOT sensors + probe) is committed & allow-listed in
# validate_no_private_data.py. Only the multi-GB raw source below remains local.
datasets/08_public_ITS_VA_1-95/

# ---- python build artifacts ----
build/
dist/
*.egg-info/

# ---- archives + oversized artifacts (keep raw/large data out of the repo) ----
*.zip
templates/dashboard_cbi.html

# ---- vendored plot4gmns (external package; pip install plot4gmns, or keep locally) ----
plot4gmns-main/
p4g_fig_results/
# heavy external-call output; use renderers/gmns_figures.py (native, integrated)
docs/p4g_gallery/

# ---- restricted-data-coupled artifacts (never ship) ----
# i17_dashboard.html embeds I-17 CBI data; render_corridor.py reads datasets/06_nvta_am_PRIVATE/ (NVTA/INRIX)
templates/i17_dashboard.html
ai-gen/variants/render_corridor.py

# ITS I-95 hub dashboard: generated artifact; hosted copy at docs/portal_demo/i95/, regen via its_datahub.py
datasets/08_public_ITS_VA_1-95_sample/its_datahub.html
